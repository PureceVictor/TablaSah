[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "licenta_sah"
version = "0.1.0"
description = "Aplicatie de sah pentru licenta"
dependencies = [
    "pygame"
]

[tool.setuptools]
packages = ["app"]